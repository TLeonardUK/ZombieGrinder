// ===================================================================
//	Copyright (C) 2013 Tim Leonard
// ===================================================================
#ifndef _ENGINE_COMPRESSED_STREAM_
#define _ENGINE_COMPRESSED_STREAM_

/*
#include "Engine/IO/Stream.h"

class CompressedStream : public Stream
{
	MEMORY_ALLOCATOR(CompressedStream, "IO");

private:
	unsigned int m_length;
	unsigned int m_position;
	bool m_compress;

public:
	CompressedStream(bool compress);
	~CompressedStream();

	bool IsEOF();
	unsigned int Position();
	void Seek(unsigned int offset);
	unsigned int Length();
	void WriteBuffer(const char* buffer, int offset, int length);
	void ReadBuffer(char* buffer, int offset, int length);
	void Close();
	void Flush();

};*/

#endif

